class Solution: //BRUTE
    def mySqrt(self, x: int) -> int:
        ans = 0 
        for i in range (1,x+1):
            if i*i<=x:
                ans = i
            else:
                break
        return ans
